#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../.." && pwd)"
echo "[frontend-regression] Storybook visual test runner is not configured in this repo yet."
echo "[frontend-regression] Use Playwright baseline in tests/e2e as current regression harness."
